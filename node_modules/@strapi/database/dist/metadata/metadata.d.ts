import type { Model } from '@strapi/database/src/types';
import type { ForeignKey, Index } from '@strapi/database/src/schema/types';
import type { Action, SubscriberFn } from '@strapi/database/src/lifecycles';
export interface Meta extends Model {
    columnToAttribute: Record<string, string>;
    indexes: Index[];
    foreignKeys: ForeignKey[];
    lifecycles: Partial<Record<Action, SubscriberFn>>;
}
export declare class Metadata extends Map<string, Meta> {
    get identifiers(): import("@strapi/database/src/utils/identifiers").Identifiers;
    get(key: string): Meta;
    add(meta: Meta): this;
    /**
     * Validate the DB metadata, throwing an error if a duplicate DB table name is detected
     */
    validate(): void;
    loadModels(models: Model[]): void;
}
//# sourceMappingURL=metadata.d.ts.map